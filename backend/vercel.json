{
  "buildCommand": "npm run check",
  "public": false,
  "env": ["BOT_TOKEN", "BOOKING_GROUP_ID", "GOOGLE_SHEET_ID", "GOOGLE_SERVICE_ACCOUNT_EMAIL", "GOOGLE_PRIVATE_KEY"],
  "functions": {
    "api/[...route].js": {
      "memory": 1024,
      "maxDuration": 60
    }
  }
}
