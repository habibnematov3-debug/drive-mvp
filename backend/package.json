{
  "name": "drivee-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Drivee backend for intercity ride requests",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build required'",
    "check": "node --check server.js && node --check src/bookingsRouter.js && node --check src/bot.js && node --check src/sheets.js && node --check src/validation.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "googleapis": "^144.0.0",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
